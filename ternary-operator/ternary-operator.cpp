#include <iostream>

int main() {

        /*
        // EXAMPLE 1:

        // Below is a code using if-else statements to check the grade and print the result.

        int grade = 70;

        if(grade >= 65){
                std::cout << "You passed the exam!" << '\n';
        } else {
                std::cout << "You failed the exam." << '\n';
        }

        // But, It can also be simplified using the ternary operator.

        grade >= 60 ? std::cout << "You passed the exam!" << '\n' : std::cout << "You failed the exam." << '\n';

        --------------------------------------------------------------------------------------------------------

        // EXAMPLE 2:

        // Below is a code using ternary operator to check if a number is even or odd.

        int number = 9;
        number % 2 ? std::cout << "The number is even." << '\n' : std::cout << "The number is odd." << '\n';

        --------------------------------------------------------------------------------------------------------

        // EXAMPLE 3:

        // Below is a code using ternary operator to check if a user is hungry or not.

        bool hungry = true;
        hungry ? std::cout << "You are hungry." : std::cout << "You are not hungry.";

        // This line of code above can be simplified further by using the `std::cout` directly:
        std::cout << (hungry ? "You are hungry." : "You are not hungry.") << '\n';

        */

        return 0;
}

/*
Notes:
- Ternary operator is a shorthand for if-else statements.
- Syntax: condition ? expression_if_true : expression_if_false;
- It can be used to simplify code and make it more readable.
- It is often used for simple conditions where an if-else statement would be too verbose.
- It can be used to assign values based on a condition.
- It is a good practice to use it for simple conditions, but for more complex conditions it is better to use if-else statements for clarity.
*/